name: Zizmor GitHub Actions Security Analysis

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: '51 18 * * 5' # weekly at 18:51 (UTC) on Friday
  workflow_dispatch:

permissions:
  contents: read

jobs:
  zizmor:
    name: Zizmor Security Analysis
    permissions:
      contents: read # for actions/checkout
      security-events: write # for zizmor to upload SARIF results
    uses: open-telemetry/shared-workflows/.github/workflows/zizmor.yml@93dce0eff8b818861b7ec1e1e8a111fd4c3e17c3 # pending v0.8.0
    with:
      use-cncf-hosted-runner: true
