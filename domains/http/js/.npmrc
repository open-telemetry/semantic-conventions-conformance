; npm links a local package by symlinking it, and Node then resolves that
; package's own imports from where it really lives — outside this build, where
; nothing is installed. Installing local packages as copies instead puts them
; and their dependencies in one tree, which is what makes the shared `tools/`
; packages usable from here.
install-links=true
