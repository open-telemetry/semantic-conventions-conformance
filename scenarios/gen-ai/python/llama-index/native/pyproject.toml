[project]
name = "llama-index-native-conformance"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "llama-index-core==0.14.24",
    # Held at 0.6 to match the openinference directory, whose instrumentation
    # pulls llama-index-multi-modal-llms-openai and so caps this one.
    "llama-index-llms-openai==0.6.26",
    "llama-index-observability-otel==0.6.4",
    "opentelemetry-api==1.44.0",
    "opentelemetry-sdk==1.44.0",
    "opentelemetry-exporter-otlp-proto-grpc==1.44.0",
]

[tool.uv]
package = false
