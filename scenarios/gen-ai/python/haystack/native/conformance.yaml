# Copyright The OpenTelemetry Authors
# SPDX-License-Identifier: Apache-2.0

runner: genai-conformance
instrumented_library: haystack
instrumentation_library: opentelemetry-haystack

server:
  run: genai-mock-server --port ${PORT}

env:
  OPENAI_BASE_URL: ${MOCK_SERVER_URL}/v1
  OPENAI_API_KEY: test_openai_api_key
  # Haystack's own switch for recording component inputs and outputs.
  HAYSTACK_CONTENT_TRACING_ENABLED: "true"
  # The shared programs, imported by the entry programs here the way a Java or
  # Node scenario imports the package holding them.
  PYTHONPATH: ..

scenarios:
  workflow:
    run: uv run --frozen --project . otel-conformance-python workflow.py

  invoke_agent:
    run: uv run --frozen --project . otel-conformance-python invoke_agent.py

  automatic_tool_calling:
    run: uv run --frozen --project . otel-conformance-python automatic_tool_calling.py
