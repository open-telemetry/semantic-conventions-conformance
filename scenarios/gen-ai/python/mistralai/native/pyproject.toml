[project]
name = "mistralai-native-conformance"
version = "0.0.0"
requires-python = ">=3.12"
# The OTel stack is pinned a release back: mistralai 2.9.0 caps
# opentelemetry-semantic-conventions to <0.61.
dependencies = [
    "mistralai==2.9.0",
    "opentelemetry-api==1.39.1",
    "opentelemetry-sdk==1.39.1",
    "opentelemetry-exporter-otlp-proto-grpc==1.39.1",
    "opentelemetry-distro==0.60b1",
]

[tool.uv]
package = false
