{
  "spans": {
    "http.server": [
      "client.address",
      "http.request.body.size",
      "http.request.method",
      "http.response.body.size",
      "http.response.status_code",
      "network.peer.address",
      "network.peer.port",
      "network.protocol.version",
      "server.address",
      "server.port",
      "url.path",
      "url.scheme",
      "user_agent.original"
    ]
  },
  "events": {},
  "metrics": {
    "http.server.request.body.size": [
      "http.request.method",
      "http.response.status_code",
      "http.route",
      "network.protocol.name",
      "network.protocol.version",
      "server.address",
      "server.port",
      "url.scheme"
    ],
    "http.server.request.duration": [
      "http.request.method",
      "http.response.status_code",
      "http.route",
      "network.protocol.name",
      "network.protocol.version",
      "server.address",
      "server.port",
      "url.scheme"
    ],
    "http.server.response.body.size": [
      "http.request.method",
      "http.response.status_code",
      "http.route",
      "network.protocol.name",
      "network.protocol.version",
      "server.address",
      "server.port",
      "url.scheme"
    ]
  },
  "findings": [
    {
      "id": "http_route_not_present",
      "message": "Server span 'GET /health' uses target '/health' but lacks 'http.route', so the matched route and target cardinality cannot be verified.",
      "context": {
        "kind": "server",
        "target": "/health"
      }
    },
    {
      "id": "error_type_missing_on_error",
      "message": "Span 'GET /status/{code}' has status.code='error' but is missing 'error.type'; it MUST be set when the operation fails.",
      "context": {
        "status_code": "error"
      }
    },
    {
      "id": "http_route_not_present",
      "message": "Server span 'GET /status/{code}' uses target '/status/{code}' but lacks 'http.route', so the matched route and target cardinality cannot be verified.",
      "context": {
        "kind": "server",
        "target": "/status/{code}"
      }
    },
    {
      "id": "http_route_not_present",
      "message": "Server span 'GET /users/{userId}' uses target '/users/{userId}' but lacks 'http.route', so the matched route and target cardinality cannot be verified.",
      "context": {
        "kind": "server",
        "target": "/users/{userId}"
      }
    },
    {
      "id": "http_route_not_present",
      "message": "Server span 'POST /items' uses target '/items' but lacks 'http.route', so the matched route and target cardinality cannot be verified.",
      "context": {
        "kind": "server",
        "target": "/items"
      }
    }
  ]
}
