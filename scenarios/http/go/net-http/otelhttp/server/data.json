{
  "spans": {
    "http.server": [
      "client.address",
      "http.request.body.size",
      "http.request.method",
      "http.response.body.size",
      "http.response.status_code",
      "network.peer.address",
      "network.peer.port",
      "network.protocol.version",
      "server.address",
      "server.port",
      "url.path",
      "url.scheme",
      "user_agent.original"
    ]
  },
  "events": {},
  "metrics": {
    "http.server.request.body.size": [
      "http.request.method",
      "http.response.status_code",
      "http.route",
      "network.protocol.name",
      "network.protocol.version",
      "server.address",
      "server.port",
      "url.scheme"
    ],
    "http.server.request.duration": [
      "http.request.method",
      "http.response.status_code",
      "http.route",
      "network.protocol.name",
      "network.protocol.version",
      "server.address",
      "server.port",
      "url.scheme"
    ],
    "http.server.response.body.size": [
      "http.request.method",
      "http.response.status_code",
      "http.route",
      "network.protocol.name",
      "network.protocol.version",
      "server.address",
      "server.port",
      "url.scheme"
    ]
  },
  "findings": [
    {
      "id": "http_span_name_format",
      "message": "Span 'GET /health' should be named one of [\"GET\"] according to HTTP span naming conventions.",
      "context": {
        "expected": [
          "GET"
        ],
        "kind": "server"
      }
    },
    {
      "id": "error_type_missing_on_error",
      "message": "Span 'GET /status/{code}' has status.code='error' but is missing 'error.type'; it MUST be set when the operation fails.",
      "context": {
        "status_code": "error"
      }
    },
    {
      "id": "http_span_name_format",
      "message": "Span 'GET /status/{code}' should be named one of [\"GET\"] according to HTTP span naming conventions.",
      "context": {
        "expected": [
          "GET"
        ],
        "kind": "server"
      }
    },
    {
      "id": "http_span_name_format",
      "message": "Span 'GET /users/{userId}' should be named one of [\"GET\"] according to HTTP span naming conventions.",
      "context": {
        "expected": [
          "GET"
        ],
        "kind": "server"
      }
    },
    {
      "id": "http_span_name_format",
      "message": "Span 'POST /items' should be named one of [\"POST\"] according to HTTP span naming conventions.",
      "context": {
        "expected": [
          "POST"
        ],
        "kind": "server"
      }
    }
  ]
}
