# Copyright The OpenTelemetry Authors
# SPDX-License-Identifier: Apache-2.0

runner: http-conformance
instrumented_library: aiohttp
instrumentation_library: opentelemetry-instrumentation-aiohttp-client
scenario_contract: ../../../../../../tools/http/test-client/client-telemetry.yaml

env:
  OTEL_SEMCONV_STABILITY_OPT_IN: http

server:
  run: http-mock-server --port ${PORT}

scenarios:
  client:
    run: uv run --frozen --project . otel-conformance-python scenario.py
